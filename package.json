{
	"name": "formbucket",
	"version": "0.3.1",
	"private": true,
	"license": "MIT",
	"type": "module",
	"description": "Self-hosted form backend on Cloudflare Workers: schema validation, analytics, webhooks and a pull API with cursors.",
	"scripts": {
		"build": "react-router build && node scripts/set-database-suffix.mjs",
		"migrate": "wrangler d1 migrations apply DB --local",
		"migrate:remote": "wrangler d1 migrations apply DB --remote",
		"cf-typegen": "wrangler types",
		"deploy": "npm run build && wrangler deploy",
		"dev": "npm run migrate && react-router dev",
		"preview": "npm run build && vite preview",
		"typecheck": "npm run cf-typegen && react-router typegen && tsc -b",
		"test": "tsx --test tests/*.test.ts"
	},
	"dependencies": {
		"@radix-ui/react-avatar": "^1.1.11",
		"@radix-ui/react-collapsible": "^1.1.12",
		"@radix-ui/react-dialog": "^1.1.15",
		"@radix-ui/react-dropdown-menu": "^2.1.16",
		"@radix-ui/react-label": "^2.1.8",
		"@radix-ui/react-separator": "^1.1.8",
		"@radix-ui/react-slot": "^1.2.4",
		"@radix-ui/react-switch": "^1.2.6",
		"@radix-ui/react-tabs": "^1.1.13",
		"@radix-ui/react-tooltip": "^1.2.8",
		"@tanstack/react-table": "^8.21.3",
		"class-variance-authority": "^0.7.1",
		"clsx": "^2.1.1",
		"date-fns": "^4.1.0",
		"isbot": "^5.1.34",
		"lucide-react": "^0.563.0",
		"next-themes": "^0.4.6",
		"prism-react-renderer": "^2.4.1",
		"react": "^19.2.4",
		"react-dom": "^19.2.4",
		"react-router": "^7.13.0",
		"recharts": "^2.15.4",
		"tailwind-merge": "^3.4.0"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.21.0",
		"@react-router/dev": "^7.13.0",
		"@tailwindcss/vite": "^4.1.18",
		"@types/node": "^22.19.7",
		"@types/react": "^19.2.10",
		"@types/react-dom": "^19.2.3",
		"tailwindcss": "^4.1.18",
		"tsx": "^4.21.0",
		"tw-animate-css": "^1.4.0",
		"typescript": "^5.9.2",
		"vite": "^7.3.1",
		"vite-tsconfig-paths": "^5.1.4",
		"wrangler": "^4.60.0"
	},
	"cloudflare": {
		"label": "FormBucket",
		"bindings": {
			"ADMIN_USERNAME": {
				"description": "Username for the FormBucket dashboard. If your setup flow cannot set runtime variables, skip this and add it after deploying (Worker Settings > Variables and Secrets)."
			},
			"ADMIN_PASSWORD": {
				"description": "Password for the dashboard (12+ random characters, stored as an encrypted secret). Can also be added after deploying; the app shows setup instructions until it exists."
			},
			"SESSION_SECRET": {
				"description": "Optional. Random string (32+ chars, e.g. `openssl rand -hex 32`) used to sign login cookies. If left empty a key is derived from the admin password."
			}
		}
	}
}
